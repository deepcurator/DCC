however, the length of dependencies captured by a dilated cnn is limited by its kernel size, whereas an rnn's autoregressive modeling can, in theory, capture potentially infinitely  long dependencies with a small number of parameters.