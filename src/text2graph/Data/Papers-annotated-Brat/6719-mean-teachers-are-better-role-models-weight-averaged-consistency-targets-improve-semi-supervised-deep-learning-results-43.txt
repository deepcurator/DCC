(one could evaluate all the targets periodically more than once per epoch, but keeping the evaluation span constant would require o(n 2 ) evaluations per epoch where n is the number of training examples.)