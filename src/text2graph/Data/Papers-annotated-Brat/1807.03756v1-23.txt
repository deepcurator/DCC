the categorical method is fit with amortized variational inference using a learned inference network to improve the training bound and policy gradient variance reduction baseline using soft attention.