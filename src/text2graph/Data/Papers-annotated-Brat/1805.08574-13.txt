in particular, we present a generic adaptive feed-forward layer that retains the basic structure of the standard feed-forward layer while significantly increasing the capacity to model non-linear patterns.