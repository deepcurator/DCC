in particular, our method is robust to handle data distributions with multiple separated modes -challenging situations where all existing gans that we have experimented with produce unwanted samples (ones that are not in any modes), whereas our method is able to generate samples spreading over all modes while avoiding unwanted samples.