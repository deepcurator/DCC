4. the supervised rendering task adds complexity to the above by requiring a network to paint a full image from the not-so-clevr dataset given the (x, y) coordinates of the center of a square in the image.