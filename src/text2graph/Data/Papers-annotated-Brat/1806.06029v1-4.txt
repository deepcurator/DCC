then, given the new sample x, we create a variational autoencoder for domain a by adapting the layers that are close to the image in order to directly fit x, and only indirectly adapt the other layers.