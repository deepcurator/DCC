we propose a convolutional neural network (cnn) architecture with a special readout layer that separates the problem of learning a common feature space from estimating each neuron's receptive field location and cell type, but can still be trained end-to-end on experimental data.