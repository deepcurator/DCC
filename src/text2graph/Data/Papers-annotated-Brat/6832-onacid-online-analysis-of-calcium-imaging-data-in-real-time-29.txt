we apply onacid to two large-scale (50 and 65 minute long) mouse in vivo 2p datasets; our algorithm can find and track hundreds of neurons faster than real-time, and outperforms the cnmf algorithm of [26] benchmarked on multiple manual annotations using a precision-recall framework.