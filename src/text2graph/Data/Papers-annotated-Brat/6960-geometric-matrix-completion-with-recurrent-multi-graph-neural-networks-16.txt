mathematically, a recommendation method can be posed as a matrix completion problem [9] , where columns and rows represent users and items, respectively, and matrix values represent scores determining whether a user would like an item or not.