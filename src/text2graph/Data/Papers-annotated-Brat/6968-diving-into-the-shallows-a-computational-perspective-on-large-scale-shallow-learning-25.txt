still, while kernel methods are easily implementable and show state-of-the-art performance on smaller datasets (see [ ] for some comparisons with dnn's) there has been significantly less progress in applying these methods to large modern data.