we develop specific instances of adaptive parameterization that can be trained end-to-end jointly with the network using standard backpropagation, are simple to implement, and run at minimal additional cost.