we also conduct extensive experiments to analyze the cnn decoder and our proposed gha, and we ﬁnd that deeper decoders cannot obtain better performance, and when the convolutional decoder becomes deeper the model is likely to collapse during training.