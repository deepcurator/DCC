but for simplicity, inspired by rsgans, we design t (xr, xf ) into the following form: t (xr, xf ) (cid:44) d(e(xr) − e(xf ))  (22) whose e(·) is an encoder for input image and d(·) is a multilayer perception with hidden difference vector of e(xr), e(xf ) as input and a scalar as output.