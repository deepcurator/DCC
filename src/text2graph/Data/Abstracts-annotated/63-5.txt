our gha signiﬁcantly improves the performance of the model that only applies one level attention, e.g., the cider score increases from 0.923 to 0.999, which is comparable to the state-of-theart models that employ attributes boosting and reinforcement learning (rl).