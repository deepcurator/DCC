we advocate laplacian k-modes for joint clustering and density mode ﬁnding, and propose a concave-convex relaxation of the problem, which yields a parallel algorithm that scales up to large datasets and high dimensions.